<ul>
  <% if @urls %>
    <% @urls.each do |url| %>
      <li><%= link_to url.short_url, url_path(url) %><span><%= url.click_count %></span></li>
    <% end %>
  <% end %>
</ul>
<h3><%= link_to  "Shorten new url", new_url_path %></h3>